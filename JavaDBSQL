CREATE TABLE users (
    id SERIAL PRIMARY KEY,        -- Works for PostgreSQL
    -- id INT PRIMARY KEY AUTO_INCREMENT,  -- Use this for MySQL
    name VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL
);
